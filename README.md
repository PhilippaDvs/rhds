# rhds
To recreate the environment used for this project, run the following code:

git clone https://github.com/PhilippaDvs/rhds.git  
cd rhds  
mamba env create -f environment.yml  
mamba activate rhds  
Rscript install.r  
